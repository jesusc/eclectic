package modelum.eclectic.jvm.test;

public class KoanCompiler {
	/*
	public JVMCompilationResult execute(String transformation) throws IOException {
		String home = "/home/jesusc/" + "usr/koan/workspace/org.eclectic.test/";		
		
		KoanMiddleEndOptions options = new KoanMiddleEndOptions();
		options.saveIntermediate = "idc.xmi";
		options.checkLocatedElements = true;
		
		JVMBackend                          b = new JVMBackend();
		KoanMiddleEnd<JVMCompilationResult> m = new KoanMiddleEnd<JVMCompilationResult>(b, options);
		KoanFrontend<JVMCompilationResult>  f = new KoanFrontend<JVMCompilationResult>(m);
		
		JVMCompilationResult executor = f.compile(home + transformation);
		return executor;
	}
	*/
}
