package org.eclectic.idc.datatypes;

import static org.junit.Assert.*;

//import org.eclectic.idc.datatypes.ImmutableList;
import org.junit.Test;

public class TestImmutableList {

	
	@Test
	public void testAdd() {
		/*
		ImmutableList l0 = new ImmutableList();
		assertEquals(true, l0.empty());
		
		ImmutableList l1 = l0.add("1");
		assertEquals("1", l1.head.value); 
		assertEquals(l1.head.last, l1.head);

		ImmutableList l2 = l1.add("2");
		assertEquals("1", l2.head.value); 
		assertEquals("2", l2.head.next.value);
		assertEquals(l2.head.last, l2.head.next);

		ImmutableList l3 = l2.add("3");
		assertEquals("1", l3.head.value); 
		assertEquals("2", l3.head.next.value);
		assertEquals("3", l3.head.next.next.value);
		assertEquals(l3.head.last, l2.head.next.next);
		*/
	}
}
